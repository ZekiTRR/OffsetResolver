# Управление смещениями и модулями процессов

Консольное приложение для Windows (x64) для управления смещениями и модулями процессов в контексте обратного инжиниринга.

**[English Version / Английская версия](../README_EN.md)**

## 🎯 Назначение

- **Защита от ASLR**: Хранение смещений в формате "модуль + смещение" вместо абсолютных адресов
- **Автоматический пересчет**: Автоматический пересчет фактических адресов при каждом запуске
- **Экспорт модулей**: Сохранение списка модулей процесса с базовыми адресами
- **Портативность**: Конфигурация работает после перезагрузки процесса/системы

---

## 🏗️ Архитектура

Приложение разделено на 5 логических модулей:

### 1. **ProcessManager**
- Поиск процесса по имени
- Получение PID процесса
- Открытие дескриптора процесса
- Управление жизненным циклом соединения

### 2. **ModuleRegistry**
- Получение списка загруженных модулей
- Сохранение ImageBase и SizeOfImage для каждого модуля
- Быстрый поиск модуля по имени (без учета регистра)

### 3. **AddressResolver**
- Пересчет абсолютного адреса: `resolved_address = module_base + offset`
- Автоматическое разрешение смещений из конфигурации
- Обработка случаев, когда модуль не найден

### 4. **OffsetStorage**
- Загрузка смещений из текстового файла
- Сохранение смещений (абсолютные адреса НЕ сохраняются)
- Формат: `имя_модуля+смещение=описание`

### 5. **ConsoleUI**
- Меню навигации
- Взаимодействие с пользователем
- Вывод таблиц и статуса

---

## 📝 Формат конфигурации

### Пример файла `offsets.cfg`:

```ini
# Файл конфигурации смещений
# Формат: ModuleName+0xOffset=Description
# Пример: client.dll+0xDEA964=LocalPlayer

client.dll+0xDEA964=LocalPlayer
client.dll+0x4DCC098=EntityList
engine.dll+0x58EFC4=ViewAngles
engine.dll+0x590DD0=ClientState
server.dll+0x2F6A20=GlobalVars
```

---

## 🚀 Использование

### Компиляция

Все файлы .cpp вместе:

```bash
cl /EHsc /std:c++17 /O2 /DUNICODE /D_UNICODE main.cpp ProcessManager.cpp ModuleRegistry.cpp AddressResolver.cpp OffsetStorage.cpp ConsoleUI.cpp
```

Или с помощью CMake / Visual Studio.

### Запуск

1. **Главное меню** — выберите режим:
   - **Управление смещениями** — управление смещениями с защитой ASLR
   - **Дампер модулей** — быстрый экспорт списка модулей (из `base_address.cpp`)

---

## 🎓 Сценарии использования

### Сценарий 1: Первый запуск (создание конфигурации)

```
1. Выберите "Управление смещениями"
2. Присоединитесь к процессу → введите имя процесса (csgo.exe)
3. Программа загружает все модули
4. Добавить новое смещение:
   - Модуль: client.dll
   - Смещение: 0xDEA964
   - Описание: LocalPlayer
5. Добавить остальные смещения
6. Сохранить смещения в файл → offsets.cfg
```

### Сценарий 2: Повторный запуск (загрузка конфигурации)

```
1. Управление смещениями
2. Присоединитесь к процессу → csgo.exe
3. Загрузить смещения из файла → offsets.cfg
4. Разрешить все смещения
5. Просмотр смещений — видим фактические адреса с учетом новой ASLR
```

### Сценарий 3: Экспорт модулей

```
1. Дампер модулей
2. Введите имя процесса
3. Список модулей отображается на экране
4. Возможно сохранить в файл
```

---

## ✅ Ключевые особенности

### Что умеет:
- ✅ Сохраняет смещения в удобном формате
- ✅ Автоматически пересчитывает адреса при запуске
- ✅ Без проблем работает с ASLR
- ✅ Без внешних зависимостей
- ✅ Готово к расширению (сканер паттернов, чтение памяти)

### Что не умеет:
- ❌ Не сохраняет абсолютные адреса
- ❌ Не читает/не пишет память процесса (пока)
- ❌ Не ищет паттерны (можно добавить)

---

## 📊 Структуры данных

### OffsetEntry
```cpp
struct OffsetEntry {
    std::wstring moduleName;     // client.dll
    uintptr_t offset;            // 0xDEA964
    std::wstring description;    // LocalPlayer
    
    // Во время выполнения (не сохраняется в файл):
    uintptr_t resolvedAddress;   // 0x7FF6A2DEA964
    bool isResolved;             // true/false
};
```

### ModuleInfo
```cpp
struct ModuleInfo {
    std::wstring name;           // client.dll
    uintptr_t baseAddress;       // 0x7FF6A2000000 (ImageBase)
    uintptr_t size;              // 0x1A3C000 (SizeOfImage)
};
```

---

## 🔧 Точки расширения

### Готово к расширению:

1. **Сканер паттернов** — добавить в `ModuleRegistry::FindPattern()`
2. **Чтение памяти** — добавить в `ProcessManager::ReadMemory()`
3. **Обновитель сигнатур** — автоматическое обновление смещений по сигнатурам
4. **Формат JSON** — вместо INI для сложных структур

Подробнее см. [API.md](API.md).

---

## ⚠️ Требования

- **ОС**: Windows 10/11 x64
- **Права**: Администратор (для некоторых процессов)
- **Компилятор**: MSVC, MinGW, Clang (с поддержкой C++11)
- **Зависимости**: WinAPI + стандартная библиотека

---

## 📂 Структура проекта

```
Process-Module-Dumper/
├── Исходные файлы
│   ├── main.cpp
│   ├── ProcessManager.h/.cpp
│   ├── ModuleRegistry.h/.cpp
│   ├── AddressResolver.h/.cpp
│   ├── OffsetStorage.h/.cpp
│   └── ConsoleUI.h/.cpp
│
├── Документация
│   ├── README.md (английская версия)
│   ├── QUICKSTART.md
│   ├── ARCHITECTURE.md
│   ├── EXAMPLES.md
│   ├── API.md
│   ├── BUILD.md
│   ├── SUMMARY.md
│   ├── VISUAL_STRUCTURE.md
│   ├── CHANGELOG.md
│   ├── INDEX.md
│   └── RUS/ (русские версии)
│
├── Конфигурация
│   ├── CMakeLists.txt
│   ├── build.ps1
│   ├── .gitignore
│   └── offsets_example.cfg
│
└── Наследие
    ├── base_address.cpp
    └── eng_base_address.cpp
```

---

## 🎓 Примеры выводов

### Список модулей:
```
====================================================
Имя модуля                          | Базовый адрес    | Размер
---------------------------------------------------------------------------
csgo.exe                            | 0x7FF7A1D00000   | 0x37E000
client.dll                          | 0x7FF6A2000000   | 0x1A3C000
engine.dll                          | 0x7FF6A5000000   | 0xB3D000
```

### Таблица смещений:
```
====================================================
Модуль               | Смещение     | Разрешенный адр | Описание
-------------------------------------------------------------------------------------
client.dll           | 0xDEA964     | 0x7FF6A2DEA964  | LocalPlayer
engine.dll           | 0x58EFC4     | 0x7FF6A558EFC4  | ViewAngles
```

---

## 📚 Документация

| Файл | Описание |
|------|---------|
| [QUICKSTART.md](QUICKSTART.md) | Быстрый старт за 60 секунд |
| [ARCHITECTURE.md](ARCHITECTURE.md) | Детальная архитектура |
| [EXAMPLES.md](EXAMPLES.md) | Примеры использования + FAQ |
| [API.md](API.md) | Справочник API |
| [BUILD.md](BUILD.md) | Инструкции сборки |
| [SUMMARY.md](SUMMARY.md) | Резюме проекта |
| [VISUAL_STRUCTURE.md](VISUAL_STRUCTURE.md) | Визуальные диаграммы |
| [CHANGELOG.md](CHANGELOG.md) | История версий |
| [INDEX.md](INDEX.md) | Индекс документации |

**Английские версии**: [../](../)

---

## 📜 Лицензия

Создано для образовательных целей в обратном инжиниринге.  
Используйте ответственно и в соответствии с законодательством.

---

## 👤 Автор

Разработано как инструмент для анализа и отладки процессов игр.

---

**Управление смещениями и модулями процессов v1.0.0**

🔍 **Успешного реверсинга!** 🚀
