# Инструмент управления модулями и смещениями процессов

Консольное приложение для Windows (x64) для управления смещениями, цепочками указателей и модулями процессов.

**[English Version / Английская версия](../README_EN.md)**

## 🎯 Назначение

- **Защита от ASLR**: Хранение смещений в формате "модуль + смещение" вместо абсолютных адресов
- **Разрешение цепочек указателей**: Разрешение многоуровневых цепочек с автоматическим чтением памяти
- **Автоматический пересчёт**: Автоматический пересчёт адресов при каждом запуске
- **Экспорт модулей**: Сохранение списка модулей с базовыми адресами
- **Режим отладки**: Встроенная система отладки с цветным выводом и логированием в файл
- **Портативность**: Конфигурация работает после перезагрузки

---

## 🏗️ Архитектура

Приложение разделено на 8 модулей:

| Модуль | Назначение |
|--------|------------|
| **ProcessManager** | Поиск процесса, получение PID, управление дескриптором |
| **ModuleRegistry** | Перечисление модулей, хранение ImageBase/Size |
| **AddressResolver** | Вычисление абсолютных адресов из модуль+смещение |
| **OffsetStorage** | Загрузка/сохранение смещений в INI формате |
| **MemoryReader** | Безопасное чтение памяти с валидацией |
| **PointerChainResolver** | Разрешение многоуровневых цепочек указателей |
| **PointerChainStorage** | Сохранение/загрузка цепочек указателей |
| **DebugLog** | Отладочный вывод с цветами и логированием в файл |
| **ConsoleUI** | Интерактивное меню |

---

## 📝 Форматы конфигурации

### Конфигурация смещений (`offsets.cfg`):

```ini
# Формат: ModuleName+0xOffset=Description
app.dll+0xDEA964=Pointer1
app.dll+0x4DCC098=Pointer2
module2.dll+0x58EFC4=Pointer3
```

### Конфигурация цепочек указателей (`chains.txt`):

```
# Формат: moduleName|baseOffset|offsets|valueType|description
app.dll|0x17E0A8|0xEC|int|Health
app.dll|0x17E0A8|0xF0|int|MaxHealth
app.dll|0x17E0A8|0x18,0x70,0x2D0|float|PositionX
```

---

## 🚀 Быстрый старт

### Сборка
```powershell
.\build.ps1
```

### Запуск
```
ProcessModuleManager.exe
```

### Главное меню
```
1. Менеджер смещений    - управление смещениями с защитой ASLR
2. Менеджер цепочек     - разрешение многоуровневых указателей
3. Дампер модулей       - экспорт списка модулей

Команды: 'debug' - переключить отладку | 'debugfile' - логирование в файл
```

---

## 🎓 Сценарии использования

### Сценарий 1: Управление смещениями

```
1. Выберите "Менеджер смещений"
2. Подключитесь к процессу → example.exe
3. Добавьте смещение:
   - Модуль: app.dll
   - Смещение: 0xDEA964
   - Описание: MyPointer
4. Сохраните в файл → offsets.cfg
5. При следующем запуске: загрузить → разрешить → увидеть новые адреса
```

### Сценарий 2: Разрешение цепочек указателей

```
1. Выберите "Менеджер цепочек"
2. Подключитесь к процессу → example.exe
3. Добавьте цепочку:
   - Модуль: app.dll
   - Базовое смещение: 0x17E0A8
   - Смещения: 0xEC (или 0x18,0x70,0x2D0 для многоуровневых)
   - Тип: int
4. Разрешите цепочки → просмотрите значения
```

### Сценарий 3: Режим отладки

```
1. Введите "debug" в главном меню → включить детальное логирование
2. Введите "debugfile" → также сохранять в debug_log.txt
3. Выполняйте операции → смотрите пошаговые логи
4. Проверьте debug_log.txt для истории
```

---

## 🔧 Система отладки

Встроенная отладка обеспечивает:

- **Цветной вывод**: Info (голубой), Success (зелёный), Warning (жёлтый), Error (красный)
- **Пошаговое логирование**: Отслеживание каждой операции
- **Трассировка адресов**: Просмотр разрешения цепочки указателей пошагово
- **Логирование в файл**: Сохранение вывода в `debug_log.txt`
- **Валидация дескрипторов**: Раннее обнаружение невалидных дескрипторов

---

## ✅ Возможности

| Функция | Статус |
|---------|--------|
| Хранение смещений (модуль+смещение) | ✅ |
| Многоуровневые цепочки указателей | ✅ |
| Чтение памяти (int/float/double) | ✅ |
| Поддержка ASLR | ✅ |
| Режим отладки | ✅ |
| Логирование в файл | ✅ |
| Без внешних зависимостей | ✅ |

---

## 📊 Структуры данных

### OffsetEntry
```cpp
struct OffsetEntry {
    std::wstring moduleName;     // app.dll
    uintptr_t offset;            // 0xDEA964
    std::wstring description;    // Pointer1
    uintptr_t resolvedAddress;   // Runtime: вычисленный адрес
    bool isResolved;             // Runtime: статус разрешения
};
```

### PointerChain
```cpp
struct PointerChain {
    std::wstring moduleName;        // app.dll
    uintptr_t baseOffset;           // 0x17E0A8
    std::vector<uintptr_t> offsets; // [0x18, 0x70, 0x2D0]
    ValueType valueType;            // INT / FLOAT / DOUBLE
    std::wstring description;       // Health
    uintptr_t resolvedAddress;      // Runtime: финальный адрес
    MemoryValue currentValue;       // Runtime: прочитанное значение
    bool isResolved;
    std::wstring lastError;
};
```

---

## 📁 Структура проекта

```
Process-Module-Dumper/
├── Исходный код (11 файлов)
│   ├── main.cpp
│   ├── ProcessManager.h/.cpp
│   ├── ModuleRegistry.h/.cpp
│   ├── AddressResolver.h/.cpp
│   ├── OffsetStorage.h/.cpp
│   ├── MemoryReader.h/.cpp
│   ├── PointerChainResolver.h/.cpp
│   ├── PointerChainStorage.h/.cpp
│   ├── ConsoleUI.h/.cpp
│   └── DebugLog.h/.cpp
│
├── Документация
│   ├── README_EN.md (Английская)
│   ├── RUS/README.md (Русская)
│   └── ...
│
└── Конфигурация
    ├── build.ps1
    ├── CMakeLists.txt
    └── offsets_example.cfg
```

---

## ⚠️ Требования

- **ОС**: Windows 10/11 x64
- **Компилятор**: MSVC (Visual Studio 2019+)
- **Права**: Администратор для некоторых процессов

---

## 📚 Документация

| Документ | Описание |
|----------|----------|
| [QUICKSTART.md](QUICKSTART.md) | Быстрый старт за 60 секунд |
| [ARCHITECTURE.md](ARCHITECTURE.md) | Архитектура системы |
| [API.md](API.md) | Справочник API |
| [BUILD.md](BUILD.md) | Инструкции по сборке |

**English**: [README_EN.md](../README_EN.md)

---

## 📜 Лицензия

Образовательный и исследовательский инструмент для реверс-инжиниринга.

---

**Process Module & Offset Management Tool v2.0**

🔍 **Удачного реверсинга!** 🚀
